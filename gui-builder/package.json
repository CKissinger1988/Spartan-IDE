{
  "name": "@spartan-ide/gui-builder",
  "version": "0.2.0-beta.1",
  "private": true,
  "license": "Apache-2.0",
  "description": "Real §6/§34 GUI Builder two-way AST sync engine (task #12) -- parses JSX/TSX into a component tree and applies structured CanvasEdit events back onto the real source AST, preserving the author's original formatting. Not yet wired into the wgpu shell (§6.1's WebView canvas) or a live dev-server bridge (§6.2 step 1) -- see README.md for exactly what this package does and does not do.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@babel/parser": "^7.26.0",
    "@babel/types": "^7.26.0",
    "ast-types": "^0.14.2",
    "esbuild": "^0.28.1",
    "recast": "^0.23.9"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
