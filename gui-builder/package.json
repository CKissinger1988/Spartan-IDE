{
  "name": "@spartan-ide/gui-builder",
  "version": "0.2.0-beta.5",
  "private": true,
  "license": "Apache-2.0",
  "description": "GUI Builder two-way AST sync engine -- parses JSX/TSX into a component tree and applies structured CanvasEdit events back onto real source while preserving author formatting. The desktop shell provides the live preview and editor bridge.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@babel/parser": "^7.26.0",
    "@babel/types": "^7.26.0",
    "ast-types": "^0.14.2",
    "esbuild": "^0.28.1",
    "recast": "^0.23.9"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
