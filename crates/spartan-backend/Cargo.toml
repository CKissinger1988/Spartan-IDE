[package]
name = "spartan-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
spartan-buffer = { path = "../spartan-buffer" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
