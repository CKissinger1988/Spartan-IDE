[package]
name = "spartan-crash"
version = "0.1.0"
edition = "2021"

[dependencies]
spartan-security = { path = "../spartan-security" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
