[package]
name = "fallback-parser-spike"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde_json = "1"

[dev-dependencies]
ureq = { version = "2", features = ["json"] }
